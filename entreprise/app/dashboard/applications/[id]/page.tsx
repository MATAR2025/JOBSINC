'use client';

import Link from 'next/link';
import { useParams } from 'next/navigation';
import { useDashboard } from '@/components/dashboard/DashboardContext';

export default function ApplicationDetailsPage() { const { id } = useParams<{ id: string }>(); const { data, loading } = useDashboard(); const application = data?.applications?.find((item) => String(item.id) === id); const name = application?.name || application?.candidateName || 'Candidat'; return <section className="application-detail-page">{loading ? <div className="dashboard-panel job-detail-skeleton" /> : !application ? <div className="dashboard-panel"><div className="jobs-empty"><h2>Cette candidature n’est pas disponible.</h2><p>Elle ne fait pas partie des candidatures accessibles pour cette entreprise.</p><Link href="/dashboard/applications" className="button button-outline">Retour aux candidatures</Link></div></div> : <><Link href="/dashboard/applications" className="back-link">← Retour aux candidatures</Link><div className="application-detail-header"><div className="candidate-avatar large">{name.slice(0, 1).toUpperCase()}</div><div><span className="dashboard-eyebrow">Profil candidat</span><h1>{name}</h1><p>{application.title || application.jobTitle || 'Poste non renseigné'}</p></div></div><div className="job-detail-grid"><div className="dashboard-panel"><h2>Informations</h2><div className="job-detail-meta"><div><span>Statut</span><strong>{application.status || 'Statut non renseigné'}</strong></div><div><span>Date de candidature</span><strong>{application.date ? new Date(application.date).toLocaleDateString('fr-FR') : '--'}</strong></div></div></div><div className="dashboard-panel"><h2>Actions</h2><p className="detail-muted">Les actions de traitement seront connectées aux endpoints existants lorsqu’ils seront disponibles.</p></div></div></>}</section>; }
