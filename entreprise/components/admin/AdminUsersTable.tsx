'use client';

import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import { AdminUserRecord, deleteAdminUser, getAdminUsers, setAdminUserBlocked } from '@/lib/admin-api';

const roles = ['Tous', 'Candidats', 'Employés', 'Entreprises / recruteurs', 'Administrateurs'];
const roleMatches: Record<string, string[]> = { Candidats: ['CANDIDATE', 'CANDIDAT', 'CANDIDATES'], Employés: ['EMPLOYEE', 'EMPLOYÉ', 'EMPLOYE'], 'Entreprises / recruteurs': ['COMPANY', 'EMPLOYER', 'RECRUITER', 'ENTREPRISE'], Administrateurs: ['ADMIN', 'SUPER_ADMIN', 'SYSTEM_ADMIN', 'MODERATOR'] };

function value(record: AdminUserRecord, key: keyof AdminUserRecord) { const v = record[key]; return (typeof v === 'string' || typeof v === 'number' ? String(v) : '') || '—'; }
function roleLabel(role?: string) { return role?.replaceAll('_', ' ') || '—'; }
function matchesRole(record: AdminUserRecord, selected: string) { if (selected === 'Tous') return true; const normalized = String(record.role || record.userType || '').toUpperCase(); return roleMatches[selected].some((role) => normalized.includes(role)); }
function statusLabel(status?: string) { if (!status || status === '—') return '—'; const map: Record<string, string> = { active: 'Actif', suspended: 'Suspendu', pending: 'En attente', blocked: 'Bloqué' }; return map[status] || status; }

export default function AdminUsersTable() {
  const [users, setUsers] = useState<AdminUserRecord[]>([]);
  const [selectedRole, setSelectedRole] = useState('Tous');
  const [query, setQuery] = useState('');
  const [page, setPage] = useState(1);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(false);
  const [actionMsg, setActionMsg] = useState('');
  const [confirmUser, setConfirmUser] = useState<AdminUserRecord | null>(null);
  const pageSize = 8;
  async function load() { setLoading(true); setError(false); try { setUsers((await getAdminUsers()) || []); } catch { setError(true); } finally { setLoading(false); } }
  useEffect(() => { const id = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(id); }, []);
  const filtered = useMemo(() => users.filter((record) => matchesRole(record, selectedRole) && `${record.firstName || ''} ${record.lastName || ''} ${record.name || ''} ${record.email || ''} ${record.company || record.companyName || ''}`.toLowerCase().includes(query.toLowerCase())), [users, selectedRole, query]);
  const pages = Math.max(1, Math.ceil(filtered.length / pageSize));
  const visible = filtered.slice((page - 1) * pageSize, page * pageSize);
  function changeRole(role: string) { setSelectedRole(role); setPage(1); }
  async function toggleBlock(record: AdminUserRecord) {
    setActionMsg('');
    try { const blocked = !record.isBlocked; await setAdminUserBlocked(String(record.id), blocked); setUsers((current) => current.map((item) => item.id === record.id ? { ...item, isBlocked: blocked, status: blocked ? 'suspended' : (item.company || item.companyName ? 'active' : 'active') } : item)); setActionMsg(blocked ? 'Utilisateur suspendu.' : 'Utilisateur réactivé.'); } catch (err) { setActionMsg(err instanceof Error ? err.message : 'Action impossible.'); }
  }
  async function doDelete() {
    if (!confirmUser) return;
    setActionMsg('');
    try { await deleteAdminUser(String(confirmUser.id)); setUsers((current) => current.filter((item) => item.id !== confirmUser.id)); setConfirmUser(null); setActionMsg('Utilisateur supprimé.'); } catch (err) { setConfirmUser(null); setActionMsg(err instanceof Error ? err.message : 'Suppression impossible.'); }
  }
  const isAdmin = (record: AdminUserRecord) => String(record.role || '').toUpperCase() === 'ADMIN' || String(record.role || '').toUpperCase() === 'SUPER_ADMIN';
  return <section className="admin-users-page"><div className="admin-page-heading"><div><p className="admin-kicker">Centre de gestion global</p><h2>Utilisateurs</h2><p>Candidats, employés, entreprises et administrateurs au même endroit.</p></div></div><div className="admin-users-toolbar"><label className="admin-users-search"><span>⌕</span><input value={query} onChange={(event) => { setQuery(event.target.value); setPage(1); }} placeholder="Rechercher un utilisateur..." aria-label="Rechercher un utilisateur" /></label><div className="admin-role-filters" role="tablist" aria-label="Filtrer par type d’utilisateur">{roles.map((role) => <button key={role} className={selectedRole === role ? 'is-active' : ''} onClick={() => changeRole(role)}>{role}</button>)}</div></div>{actionMsg ? <div className="admin-alert admin-alert-success"><strong>{actionMsg}</strong></div> : null}{error ? <div className="admin-alert admin-alert-error"><div><strong>Impossible de charger les utilisateurs.</strong><span>Vérifiez la disponibilité de l’endpoint Admin.</span></div><button className="admin-button admin-button-secondary" onClick={load}>Réessayer</button></div> : <div className="admin-card admin-table-wrap"><table className="admin-table"><thead><tr><th>Utilisateur</th><th>Email</th><th>Rôle</th><th>Statut</th><th>Entreprise</th><th>Inscription</th><th>Activité</th><th>Actions</th></tr></thead><tbody>{loading ? Array.from({ length: 5 }).map((_, index) => <tr key={index}>{Array.from({ length: 8 }).map((__, cell) => <td key={cell}><span className="admin-table-skeleton" /></td>)}</tr>) : visible.length ? visible.map((record, index) => <tr key={record.id || index} className={record.isBlocked ? 'is-suspended' : ''}><td><div className="admin-table-user"><span>{String(record.name || record.firstName || record.email || '?').slice(0, 1).toUpperCase()}</span><strong>{record.name || [record.firstName, record.lastName].filter(Boolean).join(' ') || 'Utilisateur sans nom'}</strong></div></td><td>{value(record, 'email')}</td><td><span className="admin-role-badge">{roleLabel(record.role || record.userType)}</span></td><td><span className={`admin-status-badge ${record.isBlocked ? 'admin-status-suspended' : ''}`}>{record.isBlocked ? 'Suspendu' : statusLabel(record.status)}</span></td><td>{record.company || record.companyName || '—'}</td><td>{record.createdAt || record.registeredAt || '—'}</td><td>{record.lastActivity || record.lastLogin || '—'}</td><td><div className="admin-table-actions">{isAdmin(record) ? <span className="admin-action-disabled" title="Un administrateur ne peut pas être modifié">—</span> : <><Link className="admin-table-action" href={`/admin/users/${record.id}`}>Voir</Link>{record.isBlocked ? <button className="admin-table-action" onClick={() => toggleBlock(record)}>Réactiver</button> : <button className="admin-table-action" onClick={() => toggleBlock(record)}>Bloquer</button>}<button className="admin-table-action admin-table-action-danger" onClick={() => setConfirmUser(record)}>Supprimer</button></>}</div></td></tr>) : <tr><td colSpan={8}><div className="admin-table-empty"><strong>{query || selectedRole !== 'Tous' ? 'Aucun utilisateur trouvé.' : 'Aucun utilisateur disponible.'}</strong><span>Les résultats apparaîtront dès que des comptes seront enregistrés.</span></div></td></tr>}</tbody></table>{!loading && filtered.length > 0 ? <div className="admin-pagination"><span>{filtered.length} résultat{filtered.length > 1 ? 's' : ''}</span><div><button disabled={page <= 1} onClick={() => setPage((current) => current - 1)}>Précédent</button><b>{page} / {pages}</b><button disabled={page >= pages} onClick={() => setPage((current) => current + 1)}>Suivant</button></div></div> : null}</div>}{confirmUser ? <div className="admin-modal-overlay" onClick={() => setConfirmUser(null)}><div className="admin-modal" onClick={(event) => event.stopPropagation()}><p className="admin-kicker">Action irréversible</p><h3>Supprimer cet utilisateur ?</h3><p>Le compte <strong>{confirmUser.email}</strong> sera définitivement supprimé avec ses candidatures, offres et données associées. Cette action est irréversible.</p><div className="admin-modal-actions"><button className="admin-button admin-button-secondary" onClick={() => setConfirmUser(null)}>Annuler</button><button className="admin-button admin-button-danger" onClick={doDelete}>Supprimer définitivement</button></div></div></div> : null}</section>;
}