'use client';

import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { apiRequest } from '@/lib/api';

type JobForm = { title: string; department: string; contractType: string; location: string; workMode: string; experience: string; salaryMin: string; salaryMax: string; currency: string; deadline: string; description: string; responsibilities: string; skills: string };
const initialForm: JobForm = { title: '', department: '', contractType: '', location: '', workMode: '', experience: '', salaryMin: '', salaryMax: '', currency: 'FCFA', deadline: '', description: '', responsibilities: '', skills: '' };

export default function NewJobForm() {
  const [form, setForm] = useState(initialForm); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [loading, setLoading] = useState(false); const [preview, setPreview] = useState(false);
  function update(name: keyof JobForm, value: string) { setForm((current) => ({ ...current, [name]: value })); setError(''); setSuccess(''); }
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(''); setSuccess(''); if (!form.title.trim() || !form.contractType || !form.location.trim() || !form.description.trim() || !form.skills.trim()) { setError('Complétez les champs obligatoires avant de publier l’offre.'); return; } if (form.salaryMin && form.salaryMax && Number(form.salaryMin) > Number(form.salaryMax)) { setError('Le salaire minimum doit être inférieur au salaire maximum.'); return; } const endpoint = process.env.NEXT_PUBLIC_CREATE_JOB_ENDPOINT; if (!endpoint) { setError('Le formulaire est prêt, mais l’endpoint de création d’offre n’est pas encore configuré.'); return; } setLoading(true); try { await apiRequest(endpoint, { method: 'POST', body: JSON.stringify({ ...form, salaryMin: form.salaryMin || null, salaryMax: form.salaryMax || null }) }); setSuccess('Votre offre a été créée avec succès.'); setForm(initialForm); } catch (err) { setError(err instanceof Error ? err.message : 'Impossible de créer l’offre.'); } finally { setLoading(false); } }
  const input = (name: keyof JobForm, label: string, type = 'text', required = false, placeholder = '') => <label className="job-form-field"><span>{label}{required && ' *'}</span><input type={type} value={form[name]} onChange={(event) => update(name, event.target.value)} placeholder={placeholder} required={required} /></label>;
  return <div className="new-job-layout"><div className="new-job-main"><Link href="/dashboard/jobs" className="back-link">← Retour à mes offres</Link><div className="job-form-heading"><div><span className="dashboard-eyebrow">Espace recrutement</span><h1>Créer une nouvelle offre</h1><p>Présentez clairement le poste pour attirer les bons talents.</p></div></div><form className="job-form" onSubmit={submit}><section className="dashboard-panel"><div className="job-form-section-title"><h2>Informations principales</h2><span>Les champs marqués d’un * sont obligatoires.</span></div><div className="job-form-grid">{input('title', 'Titre du poste', 'text', true, 'Ex. Développeur Full Stack')}{input('department', 'Département', 'text', false, 'Ex. Produit & Technologie')}<label className="job-form-field"><span>Type de contrat *</span><select value={form.contractType} onChange={(event) => update('contractType', event.target.value)} required><option value="">Sélectionner</option><option>Temps plein</option><option>Temps partiel</option><option>CDD</option><option>Freelance</option><option>Stage</option></select></label>{input('location', 'Localisation', 'text', true, 'Ex. Dakar, Sénégal')}<label className="job-form-field"><span>Mode de travail</span><select value={form.workMode} onChange={(event) => update('workMode', event.target.value)}><option value="">Sélectionner</option><option>Sur site</option><option>Hybride</option><option>À distance</option></select></label><label className="job-form-field"><span>Niveau d’expérience</span><select value={form.experience} onChange={(event) => update('experience', event.target.value)}><option value="">Sélectionner</option><option>Débutant</option><option>Intermédiaire</option><option>Confirmé</option><option>Expert</option></select></label></div></section><section className="dashboard-panel"><div className="job-form-section-title"><h2>Rémunération et échéance</h2><span>Facultatif selon votre politique de recrutement.</span></div><div className="job-form-grid job-form-grid-four">{input('salaryMin', 'Salaire minimum', 'number', false, '0')}{input('salaryMax', 'Salaire maximum', 'number', false, '0')}<label className="job-form-field"><span>Devise</span><select value={form.currency} onChange={(event) => update('currency', event.target.value)}><option>FCFA</option><option>EUR</option><option>USD</option></select></label>{input('deadline', 'Date limite', 'date')}</div></section><section className="dashboard-panel"><div className="job-form-section-title"><h2>Description du poste</h2><span>Décrivez le rôle, les attentes et le profil recherché.</span></div><label className="job-form-field"><span>Description *</span><textarea value={form.description} onChange={(event) => update('description', event.target.value)} maxLength={3000} rows={7} required placeholder="Présentez le poste et son impact dans l’entreprise…" /><small>{form.description.length} / 3000</small></label><label className="job-form-field"><span>Responsabilités</span><textarea value={form.responsibilities} onChange={(event) => update('responsibilities', event.target.value)} rows={5} placeholder="Une responsabilité par ligne…" /></label><label className="job-form-field"><span>Compétences recherchées *</span><textarea value={form.skills} onChange={(event) => update('skills', event.target.value)} rows={4} required placeholder="Ex. React, TypeScript, communication…" /></label></section>{error && <div className="form-error" role="alert">{error}</div>}{success && <div className="form-success" role="status">{success}</div>}<div className="job-form-actions"><Link href="/dashboard/jobs" className="button button-outline">Annuler</Link><button type="button" className="button button-outline" onClick={() => setPreview(true)}>Prévisualiser</button><button type="submit" className="button button-primary" disabled={loading}>{loading ? 'Publication…' : 'Publier l’offre'}</button></div></form></div>{preview && <aside className="job-preview"><div className="preview-header"><span>Aperçu</span><button type="button" onClick={() => setPreview(false)} aria-label="Fermer l’aperçu">×</button></div><div className="job-preview-card"><span className="dashboard-eyebrow">{form.contractType || 'Type de contrat'}</span><h2>{form.title || 'Titre du poste'}</h2><p>{form.location || 'Localisation'} · {form.workMode || 'Mode de travail'}</p><hr /><h3>À propos du poste</h3><p className="preview-text">{form.description || 'La description de votre offre apparaîtra ici.'}</p><h3>Compétences</h3><p className="preview-text">{form.skills || 'Les compétences recherchées apparaîtront ici.'}</p></div></aside>}</div>;
}
